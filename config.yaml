name: Git Sync Agent
version: "1.0.0"
slug: git_sync_agent
description: Sincroniza carpetas de GitHub a Home Assistant de forma automática.
startup: application
boot: auto
host_network: false
init: false
options:
  repo_url: "https://${GITHUB_TOKEN}@github.com/Rob-Negrete/git-sync-agent.git"
  branch: "main"
  sync_interval: 300
  target_dir: "/config/sync"
schema:
  repo_url: str
  branch: str
  sync_interval: int
  target_dir: str
image: "ghcr.io/home-assistant/amd64-base"